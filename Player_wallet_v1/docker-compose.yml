version: '3.8'

services:
  weather-api:
    image: Player_wallet_v1
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5054:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
